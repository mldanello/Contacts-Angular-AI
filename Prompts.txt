create an angular application to list, add, edit, delete contacts using a restful contacts API with a token endpoint that takes a user name and password to return a bearer token for authentication to the api.  I want the angular app to use standalone components , angular best practices, with a menu bar with options for Home and Contacts, a Home page with a graphic for MD Management Studio and a contacts page that lists contacts and has a contact detail pane with can also be used to edit the contact

refactor the contact component.  Change the list to use a ContactList model that includes properties for, id as int, firstName as string, lastName as string.  Change the contact detail pane to perform an API Get at /contacts/#Id.  Change the properties of the Contact Detail pane to include:
id: int readonly,
firstName: string not required
middleName: string not required,
lastName: string required,
companyName: string not required,
email: string valid email
web: string not required,
notes: string use scrollable text area
isActive: boolean readonly
createdAt: datetime readonly
modifiedAt: datetime readonly
and put the models in separate typescript files